<h1>Lista de usuarios</h1>
<ul>
    {{#each lista as | user |}}
    <section>
        <div>
            <h2>{{user.name}}</h2>
            <h2>{{user.age}}</h2>
            <h2>{{user.job}}</h2>
        </div>
    </section>
</ul>
{{/each}}